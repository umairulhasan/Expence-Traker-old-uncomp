import React from "react";

export default function propsPractice(props) {
  return (
    // in back ground prop is use as a object

    // props {

    //  dishName :'This is the parent data'

    // }
    <div>
      <h1>Hello {props.displayName}</h1>
    </div>
  );
}
